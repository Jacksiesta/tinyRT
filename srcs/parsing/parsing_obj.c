#include "../../includes/miniRT.h"

int	parsing_sphere(t_scene **scene, char *line)
{
	t_vector	*center;
	float		radius;
	int		color;
	int		x[3];
	
	x[0] = 2;
	x[1] = -1;
	x[2] = 0;
	while (++x[1] < 4 && ())		
	
}
